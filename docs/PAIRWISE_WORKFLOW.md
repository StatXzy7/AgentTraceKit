# Pairwise workflow

Each pair has two distinct session IDs, exact prompt text, provider, policy, side bindings, and a human review sidecar. Bindings point to immutable bundle revisions. A conclusion is only accepted as `A 更好`, `Same`, or `B 更好`; the tool never infers a winner or writes a reason.
