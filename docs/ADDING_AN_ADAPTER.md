# Adding an adapter

Implement `AgentAdapter` in `src/agent_trace_kit/adapters/`, returning the conservative normalized event model. Preserve raw bytes and source line references. Do not infer another provider's schema from Codex.
