# Configuration

Configuration is UTF-8 TOML. Defaults are overridden by config files and explicit CLI values. Unknown keys and invalid enums fail validation. Secret values are never printed; use environment variable references. `pairwise_gsb_20260916` is the built-in policy identifier.
